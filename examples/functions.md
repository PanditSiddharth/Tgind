```js
send(text, options)   
edit(message_id, text, options)   
del(message_id)  or del()
mute(user_id, options)  
unmute(user_id, options)  
ban(user_id, options)  
unban(user_id, options)  
promote(user_id, options)  
demote(user_id, options)  
getMember(userid, options)  
pin(message_id, options)  
unpin(message_id, options)  
unpinAll(options)  
getChat(options)  
getMe()
getAdmins(options)  
getMemberCount(options)  
leaveChat(options)  
answerCallbackQuery(callback_query_id, options)  
approveJoinRequest(user_id, options)  
declineJoinRequest(user_id, options)  
forward(from_chat_id, message_id, options)  
copy(from_chat_id, message_id, options)  
sendPhoto( new InputFile(path, filename), options) // import class InputFile first by let InputFile = require(tgind/classes)
sendAudio( new InputFile(path, filename), options)  // import class InputFile first by let InputFile = require(tgind/classes)
sendVideo( new InputFile(path, filename), options)  // import class InputFile first by let InputFile = require(tgind/classes)
sendLocation( latitude, longitude, options)  
sendDoc(new InputFile(path, filename), options)  // import class InputFile first by let InputFile = require(tgind/classes)
sendVenue( latitude, longitude, title, address, options)  
sendLocation( phone_number, first_name, options)  
sendPoll( question, your_options, options)  
sendDice(options)  
exportChatInviteLink(options)  
link(options)  
revokeLink(options)  
sendAction(action, options)  
getUserProfilePhotos(user_id, options)  
getFile(file_id, options)  
deleteWebhook(optional_dropUpdates)  
```  
